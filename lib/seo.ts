/**
 * SEO Utilities — stubs for build
 */
import type { Metadata } from 'next';

export function generateMetaTitle(_data: unknown): string {
  return '1Way Home Services | Tax Preparation & Real Estate';
}

export function generateMetadata(_data: unknown): Metadata {
  return {};
}

export function generateLocalBusinessSchema(): string {
  return '';
}

export function generatePageContent(): unknown {
  return {};
}
